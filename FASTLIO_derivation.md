# FAST-LIO 公式推导与代码对照

> 对照材料:`_fastlio课件.pdf`(第 74–81 页「残差的计算 / ESKF 观测方程 / 观测方程雅可比推导 / ESKF 迭代更新 / 高斯牛顿法回顾 / 新卡尔曼增益公式推导」/ 第 81 页代码解析)
> 对照代码:[include/esekfom.hpp](include/esekfom.hpp) 的 `h_share_model()` 与 `update_iterated_dyn_share_modified()`
> 行号会随代码改动漂移,以函数名为准。

---

## 一、记号对齐

| 课件符号 | 代码 | 含义 |
|---|---|---|
| `^L p` | `point_` / `p_body` | 雷达系下的特征点 |
| `^I R_L`, `^I T_L` | `x_.offset_R_L_I`, `x_.offset_T_L_I` | 雷达 → IMU 外参 |
| `^G R_I`, `^G T_I` | `x_.rot`, `x_.pos` | IMU 在世界系的位姿 |
| `p`(式 9) | `p_global` / `point_world` | 转到 G 系的点 |
| `n̄`, `p_a` | `norm_p`(前 3 维), `norm_p.intensity = pd2` | 平面单位法向量、点到面距离 |
| `x̃` | `vectorized_state dx`(24×1) | 误差状态 |

状态量顺序(由 `boxplus` 决定,[esekfom.hpp:66-81](include/esekfom.hpp#L66-L81)):

```
x = [ p(0:3) | R(3:6) | R_L(6:9) | t_L(9:12) | v(12:15) | bg(15:18) | ba(18:21) | g(21:24) ]
```

**这一步很关键** —— 它决定了代码 H 矩阵 12 列的排列方式。

---

## 二、残差的计算(课件式 3、4、9、10 ↔ 代码 117、143 行)

**课件式 (9):** `p = ^G R_I (^I R_L ^L p + ^I T_L) + ^G T_I`

代码 [esekfom.hpp:117](include/esekfom.hpp#L117) 逐字对应:

```cpp
V3D p_global(x_.rot * (x_.offset_R_L_I * p_body + x_.offset_T_L_I) + x_.pos);
//            ^G R_I      ^I R_L        ^L p        ^I T_L            ^G T_I
```

**课件式 (10):** `h = (p − p_a) · n̄`,即点到面的有符号距离。

代码 [esekfom.hpp:143](include/esekfom.hpp#L143) 算的是 `pd2 = n·p_world + d`。细节:`esti_plane` 解的是 `A/D·x + B/D·y + C/D·z + 1 = 0`,再整体除以 `n = ‖(A,B,C)‖/D` 得到 `(a,b,c,d)`,满足 `a²+b²+c²=1`、`d = 1/n`(见 [common_lib.h:92-99](include/common_lib.h#L92-L99))—— 正是课件第 81 页的「参数归一化」。

所以 `pd2 = n̄·p + d = n̄·(p − p_a) = h`,**就是式 (10) 的残差**,且 `n̄` 是单位法向量。

> 课件第 81 页的筛选条件 `1 − 0.9·d_面/‖p‖ > 0.9  ⟺  d_面 < (1/9)·‖p‖`,即"距离原点越远的点,容差越大",对应代码 [esekfom.hpp:144-146](include/esekfom.hpp#L144-L146) 的 `s = 1 - 0.9*fabs(pd2)/sqrt(p_body.norm())`。

---

## 三、雅可比 H 的推导(课件式 8、11、12、13 ↔ 代码 179–206 行)⭐

### 3.1 链式法则(式 8)

```
H = ∂h/∂p · ∂p/∂x̃
```

**式 (11):** `∂h/∂p = n̄ᵀ`(1×3 行向量)—— 因为 `h = n̄ᵀp − n̄ᵀp_a`。

### 3.2 ∂p/∂x̃(式 12)—— 右扰动模型

设 `X ≜ ^I R_L ^L p + ^I T_L`(点在 IMU 系下的表达),则 `p = ^G R_I X + ^G T_I`。

**(a) 对旋转 `^G R_I` 右扰动** `^G R_I ← ^G R_I exp(δθ)`:

```
p(R exp(δθ)) ≈ ^G R_I (I + δθ^∧) X + ^G T_I
             = p + ^G R_I (δθ^∧ X)
             = p − ^G R_I X^∧ δθ

⟹  ∂p/∂δθ = − ^G R_I X^∧          ← 课件式(12)第 1 块,带负号
```

**(b) 对位置 `^G T_I`:**

```
∂p/∂^G T_I = I₃                    ← 课件式(12)第 2 块
```

**(c) 对外参旋转 `^I R_L`(课件没写,S-FAST-LIO 新增)** `^I R_L ← ^I R_L exp(δθ_L)`:

```
X ≈ ^I R_L ^L p + ^I R_L δθ_L^∧ ^L p + ^I T_L
  = X − ^I R_L (^L p)^∧ δθ_L

⟹  ∂p/∂δθ_L = − ^G R_I ^I R_L (^L p)^∧
```

**(d) 对外参平移 `^I T_L`:**

```
∂p/∂^I T_L = ^G R_I
```

> 代码 `boxplus` 用 `R ← R·exp(δ)`、`boxminus` 用 `Log(R₂ᵀR₁)`,正是**右扰动约定**,与课件式(12)「利用了李代数求导的右扰动模型」完全一致。

### 3.3 组装 H(式 13 ↔ 代码)

记 `C ≜ ^I R_G n̄ = (^G R_I)ᵀ n̄`(法向量在 IMU 系下的表达)。把式(11) 分别左乘式(12) 各块,再**转置成列向量**(代码存的是列):

**位置块**

```
n̄ᵀ  →  转置即 n̄
```

**旋转块** `−n̄ᵀ ^G R_I X^∧`,利用 `(X^∧)ᵀ = −X^∧`(反对称):

```
(−n̄ᵀ ^G R_I X^∧)ᵀ = −(X^∧)ᵀ ^G R_Iᵀ n̄ = +X^∧ C = A        ✅
```

**外参旋转块** `−n̄ᵀ ^G R_I ^I R_L (^L p)^∧`,同理:

```
⟹ᵀ = +(^L p)^∧ ^L R_I ^I R_G n̄ = B                       ✅
```

**外参平移块**

```
(n̄ᵀ ^G R_I)ᵀ = ^G R_Iᵀ n̄ = C
```

所以下面这段代码

```cpp
V3D C(x_.rot.matrix().transpose() * norm_vec);                     // ^I R_G n̄
V3D A(point_I_crossmat * C);                                       // X^∧ C
V3D B(point_crossmat * x_.offset_R_L_I.matrix().transpose() * C);  // (^L p)^∧ ^L R_G n̄
ekfom_data.h_x.block<1, 12>(i, 0) << norm_p.x, norm_p.y, norm_p.z,
    VEC_FROM_ARRAY(A), VEC_FROM_ARRAY(B), VEC_FROM_ARRAY(C);
```

就是

```
H(i,:) = [ n̄ᵀ | (X^∧ C)ᵀ | ((^L p)^∧ ^L R_G n̄)ᵀ | (^I R_G n̄)ᵀ | 0 ⋯ 0 ]

       = [ n̄ᵀ | −n̄ᵀ ^G R_I X^∧ | −n̄ᵀ ^G R_I ^I R_L (^L p)^∧ | n̄ᵀ ^G R_I | 0 ⋯ 0 ]
```

**几何意义**

| 块 | 表达式 | 含义 |
|---|---|---|
| A | `X^∧ C` | 杠杆臂(X)× 法向量(I 系) |
| B | `(^L p)^∧ ^L R_G n̄` | 雷达点(^L p)× 法向量(L 系) |
| C | `^I R_G n̄` | 法向量转到 IMU 系 |

都是"点/臂 叉乘 法向"的经典 LOAM 形式。

### 3.4 与课件式(13)的差异

课件写的是

```
H = [−n̄ ^G R̂_I (^I R_L ^L p + ^I T_L)^∧ , n̄ , 0 , 0 , 0 , 0]
```

即**旋转块在第 1–3 列、位置块在后 3 列**;代码是**位置块在前**。内容一字不差 —— 代码的 `A` 就是课件第 1 块的转置,转置那一步正好靠 `(X^∧)ᵀ = −X^∧` 消化掉课件的负号,只是分块顺序跟着 `boxplus` 的状态序走。

另外课件只到 18 维(不含外参),代码是 24 维、H 取前 12 列,多出的 7–9 / 10–12 列就是外参旋转 `B` 和外参平移 `C`。

### 3.5 残差符号

[esekfom.hpp:206](include/esekfom.hpp#L206) 的

```cpp
ekfom_data.h(i) = -norm_p.intensity;   // = −z = −pd2
```

存的是 `−z`。课件式(7)把观测写成 `0 = z + Hx̃ + v`,迭代里用的是 `−Kz`,所以代码取负号,正好对上 4.2 的更新式。

---

## 四、ESKF 迭代更新(课件式 17–19、26–39 ↔ 代码 230–296 行)

### 4.1 卡尔曼增益 K —— 课件式 (36) + 分块技巧(第 81 页)

`R` 是标量 `LASER_POINT_COV = 0.001`([laserMapping.cpp:29](src/laserMapping.cpp#L29)),所以 `HTH/R ≡ HᵀR⁻¹H`:

```cpp
HTH.block<12, 12>(0, 0) = H.transpose() * H;          // 只前 12 列非零
                                                      //  课件: H^T R^-1 H = [[H'^T R^-1 H', 0], [0, 0]]
auto K_front = (HTH / R + P_.inverse()).inverse();    // 式(36): (H^T R^-1 H + P^-1)^-1
K = K_front.block<24, 12>(0, 0) * H.transpose() / R;  // 式(36): … · H^T R^-1
```

对应课件第 81 页:`K = K_front · [H'ᵀ; 0]/R = [[K_front₁₂ₐ H'ᵀ], [K_front₁₂ᵦ H'ᵀ]]/R` —— 代码用 `block<24,12>` 取前 12 列,一字对应。

课件式(37)–(39) 证明它与传统 `K = PHᵀ(HPHᵀ+R)⁻¹` 等价,但把 `m×m` 求逆换成 `12×12`,这也是课件 TABLE II 里快 100 倍的原因。

### 4.2 状态更新 —— 式 (34)(35)

```cpp
dx_new = boxminus(x_, x_propagated);          // X ≜ x̂^κ ⊟ x̂,式(15)
dx_ = K * dyn_share.h + (KH - I) * dx_new;    // = −Kz − (I − KH)X
x_ = boxplus(x_, dx_);
```

把 `h = −z` 代入,`K·h = −Kz`,于是

```
dx_ = −Kz − (I − KH)·X
```

正是课件式(34) 末行的 `Δx̃^κ = −Kz^κ − (I − KH)J^{κ−1}X`,**代码取 J^κ = I**(小扰动下 J 极接近单位阵,标准简化),也是 FAST-LIO 论文式(18) 的形式。

> **关于课件式(34)(35) 里多出的 `−x̃^κ` 项**:那是"累计增量"的记账项 —— `x̃^{κ+1} = x̃^κ + Δx̃^κ` 是在**累加**上一轮已经加过的增量。代码每轮都用当前的 `x^κ` 直接从零重算全量增量(`dx_new` 里已经含了累计误差),而且 `h_share_model` 每次都按当前 `x^κ` 重算残差 `z^κ`(把 `HJδ` 部分吸进了残差),所以不需要这一项。两者在 κ=0 处完全重合,κ≥1 时也等价。

`x_ = boxplus(x_, dx_)` 对应式(35):误差状态更新 `x̃^{κ+1} = x̃^κ + Δx̃^κ` 等价于直接在 `x^κ` 上 ⊞ 增量。

### 4.3 协方差更新 —— 式 (19)(30)(32)

```cpp
P_ = (Matrix<double, 24, 24>::Identity() - KH) * P_;   // (I − KH)P = KRH^T
```

课件式(30) 用 SMW 公式推出 `(JJᵀ)⁻¹ = (I − KH)P`;式(32) 从 `KR = (I−KH)PHᵀ` 推出 `(I−KH)P = KRH⁻ᵀ`。代码用的就是这个 `(I−KH)P` 形式。

### 4.4 收敛判断

[esekfom.hpp:274-281](include/esekfom.hpp#L274-L281) 的 `‖dx_‖∞ < epsi`(epsi = 0.001),对应课件第 77 页高斯牛顿步骤 4「若 Δx 足够小则停止,否则返回 2 继续迭代」。

### 4.5 迭代控制

- `dyn_share.converge` 为 true 时才重新做最近邻搜索(课件第 81 页流程图),否则复用上一次的邻域;
- 连续收敛 2 次(`t > 1`)或达到最大迭代次数就退出,并更新 P;
- 迭代 3 次仍未收敛时强制置 `converge = true`,避免卡死。

---

## 五、对照结论

| 课件 | 代码 | 一致性 |
|---|---|---|
| (9)(10) 残差 | 117、143 行 | ✅ 完全一致 |
| (11) `∂h/∂p = n̄ᵀ` | `norm_p` | ✅ |
| (12) `∂p/∂x̃` | A / C 两个块 | ✅ 右扰动约定一致 |
| (13) H | 198 / 202 行 | ✅ 内容一致(块序不同;代码多外参块 B) |
| — 外参雅可比 | B 块 | ➕ 代码扩展,课件无 |
| (34)(35) 迭代更新 | 269、271 行 | ✅ 取 J = I 的简化 |
| (36)(39) 新增益 K | 263-265 行 | ✅ 含分块技巧 |
| (19) P 更新 | 293 行 | ✅ |
| 第 81 页筛选阈值 | 144-146 行 | ✅ |

### 四处需要注意的不同

1. **状态序**:课件把旋转块排在前 3 列,代码是 `[p, R, R_L, t_L]`,与 `boxplus/boxminus` 一致 —— 读代码时最容易看错的地方。
2. **维数**:课件 18 维(无外参),代码 24 维、H 只取 12 列;`extrinsic_est` 为 false 时 [esekfom.hpp:202](include/esekfom.hpp#L202) 把 6–11 列清零 —— 列 6–8 是外参旋转、9–11 是外参平移,正好整块清零。
3. **J^κ**:代码取单位阵,`(KH − I)·dx_new` 承担了 `−(I − KH)J⁻¹X` 的角色。
4. **代码注释里的编号是论文编号,不是课件编号** —— 例如 256 行注释「公式(18)中的 x^k − x^」指的是 FAST-LIO 论文的式(18),对应课件式(15)/(34);不要按课件编号去找。

---

## 六、符号自查方法

要验证 A 的符号有没有写反,最快的办法是数值检查:任取一个点,把 `p_global` 对 `x_.rot` 做一次微小右扰动 `^G R_I exp(δθ)`,用有限差分求

```
∂(n̄ᵀ p_global) / ∂δθ  =?  A
```

两者应当相等(`A` 为 3×1 列向量,差分的行向量取转置后比较)。外参块 B 同理,对 `x_.offset_R_L_I` 做右扰动即可。
